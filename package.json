{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cli": "node --enable-source-maps dist/cli/index.js",
    "cli:dev": "node --enable-source-maps --import tsx src/cli/index.ts",
    "pdf": "npm run cli -- --pdf",
    "pdf:dev": "npm run cli:dev -- --pdf",
    "smoke:versions": "node --enable-source-maps --import tsx scripts/smoke_versions.ts",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^25.2.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  },
  "dependencies": {
    "puppeteer": "^23.11.1"
  }
}
